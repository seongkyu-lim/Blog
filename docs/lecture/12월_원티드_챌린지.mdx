---
sidebar_position: 4
title: 11월_원티드_객체지향
---

- 12-07
  - 예외를 내려줄 때, http 상태 코드는 200이 좋을까 예외 코드와 동일하게 내려주는 것이 좋을까?
  - 커스텀 예외에 대해서 응답 코드만 케이스별로 상세히 나누는 것이 좋을까, 예외 클래스도 케이스별로 분리해서 구현하는 것이 좋을까?
  - 예외 시 응답 데이터
    - status, code, message.
- 12-08
  - 모니터링
    - sentry, datadog, 뉴레릭, kibana,
  - 로그
    - 어디에 로그를 찍을 것인가.
      - 예외 발생 부분, 미들웨어로 전체 호출에 대해, 로직, 컨트롤러, 유저 온클릭 요소
    - 어떤 정보를 남길 것인가.
    - 로그 레벨
      - trace, debug, info, warn, error, fatal
  - 트랜잭션
    - 4가지 원칙
    - 격리레벨
      - 트랜잭션 실행시 발생할 수 있는 문제 점들
        - dirty read -커밋에되지 않는 데이터를 읽을수 있는 문제.
        - non repeatable read -
        - phantom read
  - 동시성 제어
    - 락
      - 공유락 - 서로 다른 트랜잭션이 락이 걸렸더라도 읽을 수는 있게.
      - 베타적락 - 서로 다른 트랜잭션이 같은 데이터를 읽고 쓸수 없게함.
      - 2PL 프로토콜
- 12-15
  - 테스트 코드 의존성 범위
    - 런던파 : 모든 의존성 제거.
    - 고전파 : 공유 하는 것들에 대한 의존성만 제거(ex db)
  - 테스트 대역
    - 유형 : 더미, 스텁, 스파이, 목, 페이크
  -

---
sidebar_position: 3
title: 11월_원티드_객체지향
---

- 2024-11-07
  - 멀티 쓰레드 환경에서 싱글톤객체의 메소드에 동시에 접근하면 성능이슈가 발생할 수 있다 ?
    - 쓰레드 세이프티 하게 코드를 짜야된다
    - 주문ID 랜던 값을 얻는 메소드 호출 . 할떄 , random -> ThreadLocalRandom
    - 리소스, 인스턴스 사용에 대한부분도 멀티 쓰레드 환경에서 고려를 해야한다.
    - 락을 걸 때 데드락 조심.
  - 스트림, 람다를 사용하는 것이 병렬 처리를 지원해 좋다.
  - Controller에서 json <-> 객체 파싱되는 원리
    - requestDto, responseDto를 세팅할 때 불필요한 보일러플레이트 코드 빼자.
  - dto 유효성 검사
    - interceptor 처리 후에 validation이 동작한다.
- 2024-11-12
  - 팩토리 대신 map에다 싱글톤 객체를 모두 넣어두고 꺼내서 사용하는 기법도 있다
    - 분기문 없이 로직이 깔끔해지는 장점 잇는듯
    - 팩토리 클래스없이 그냥 메인로직에서 사용할 수 있을듯
    - 일급 컬렉션??
  - 인터페이스의 구체 메소드
    - 구체 메소드가 없다보니 해당 메소드에 수정이 필요한경우 관련된 모든 구현체에 수정을 하는 불편함이 있어서 해결하기위해 유연한 대응을 위해 나타낫다
  - 상속의 단점에 대해 설명.
    - but 공통된 필드, 엔티티, 디티오와 같은 케이스에서는 상속이 좋다.
  - ddd
    - 이벤트 스토밍
    - 에그리거트와 엔티티
    - 고수준 모듈과 저수준 모듈
